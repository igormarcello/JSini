function modulo(){
  var numero = 6; // tipo number (1, 10, 2.4 , 3.40...)
  console.log(numero);
  console.log(typeof(numero));//tipeof mostra o tipo da variavel; 

  var palavra = " ";// tipo cadeia de caracteres, pode ser uma simples letra ou um texto, o vazio tbm é lido como string;
  console.log(palavra);
  console.log(typeof(palavra));

  var logica = true;//tipo boleano,uma tomada de decisão, sendo elas verdadeiro ou falso;
  console.log(logica,typeof(logica));

}module.exports = {modulo};